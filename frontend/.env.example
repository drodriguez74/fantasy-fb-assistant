# Backend API base URL. Optional for local dev -- defaults to
# http://localhost:8000/api/v1 when unset. Required in Vercel's frontend
# project env vars so the deployed frontend calls the deployed backend
# instead of localhost, e.g.:
# VITE_API_URL=https://your-backend.vercel.app/api/v1
VITE_API_URL=
