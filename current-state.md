# Current State

_High-level project status. Read this (with `memory.md`) via `/orient` after `/clear`. Not a task queue — see `handoff.md` for the active task._

## What this project is

FastAPI + React/TS fantasy football assistant. PPR-scoped (keeper/dynasty explicitly out of scope, founder's call). Running locally against real Supabase Postgres, not deployed — see [[project_local_dev_supabase]] in memory. `docker-compose.yml` is stale, don't use it.

## Milestones (evidence-based, from repo docs + git log)

- [x] **P0 audit pass** (`AUDIT_TASK_LIST.md`) — auth was fake (login/`/me` stubs), 16 models invisible to `Base.metadata` (real data-loss risk on next migration), frontend build was broken, shared `httpx.AsyncClient` broke across event loops. All fixed.
- [x] **UX audit pass** (`UX_TASK_CHECKLIST.md`, `UX_PRODUCT_REVIEW.md`) — security holes (unauthenticated league endpoints), fabricated demo content on live analysis endpoints, blog duplicate-content bug, AI-copy honesty pass, visual identity system. 17/19 tracked items done.
- [x] **Visual identity rollout** — type system + signature "yard-divider" motif rolled out app-wide (most recent commits through `e0337b1`).
- [x] **Draft assistant hardening** — AI provider circuit breaker (`a7a5179`) + algorithmic fallback baseline (`c53df9e`) so draft recs degrade gracefully instead of 500ing when AI is down.
- [x] **ESPN roster/live-draft fixes** — season defaulting, roster grading, bench-slot bug (`8360cba`, `cd3b6bd`, `33a337a`).
- [x] **Mock draft (`DraftPage.tsx`) bug fix, 2026-08-29** — user-reported: recommendations kept pushing RB/WR causing an "overstocked" final grade, and K/DEF were never selectable. Root causes: (1) `getPositionNeeds`'s fallback `needs.length > 0 ? needs : ['RB', 'WR']` meant once RB/WR/QB/TE needs were satisfied, the code claimed RB/WR were *still* needed forever, driving the Team Needs panel, bot picks, and `generateRecommendations` requests to keep pushing RB/WR indefinitely; (2) the player pool fetch only requested RB/WR/QB/TE positional rankings — K/DEF were never in `availablePlayers` at all, despite the position filter dropdown offering them and the grading logic expecting 1 K + 1 DEF. Fixed both: `IDEAL_POSITION_COUNTS` now includes K:1/DEF:1 (matching `STANDARD_LINEUP` in `draft_recommendation_fallback.py`), the fallback now correctly returns an empty array ("no need, best player available") instead of re-injecting RB/WR, and the pool fetch now requests K/DEF rankings too. Verified live in-browser: K filter now returns real kickers; `pytest` unaffected (frontend-only change); `npm run build` green.
- [~] **ESPN live draft feed** — confirmed architecturally broken (not just untested), no known REST/WS fix exists. Manual "mark gone" mitigation shipped. Real fix (browser extension reading ESPN's DOM) not built. See [[project_espn_live_draft_broken]].
- [x] **Waiver wire personalization + dead-engine fix (2026-09-05)** — the live waiver endpoint (`GET /waiver-wire/recommendations`, `WaiverWireService.get_live_trending_recommendations`) previously ranked purely by raw Sleeper add-count with zero personalization; a second "league-aware"/"personalized" engine (`_evaluate_player_for_waiver`) was dead code (hardcoded empty performance history, so every sub-score silently defaulted to a fabricated 0.5). Fixed both: real roster-need weighting (FLEX-aware, via the same `draft_assistant._effective_position_requirements` helper other grading code uses) + real bye-week flagging + honest scoring-format context on the live endpoint; real historical-performance query restored on the dead engine, with `None`/"insufficient data" (not a fabricated default) surfaced as `data_confidence` when history is thin. `/league-aware-recommendations/{league_id}` now actually calls the live personalized method with a real fetched roster instead of the old dead local-DB path. Roster improvement recs (`post_draft_analysis_service.py::_generate_improvement_recommendations`) now surface real candidate players from the live feed instead of static templates. Frontend (`WaiverWirePage.tsx`) has a "Personalize for" league picker and per-card badges for the new signals. Committed as `6b3c230`.
- [x] **Roster Analysis strength/weakness contradiction fix (2026-09-05)** — user-reported: same position (TE) appeared as both a strength ("Solid TE situation") and a weakness ("TE is below your league's required depth") simultaneously. Root cause: `roster_grading.py::grade_roster` merged two independent generators (legacy hardcoded-threshold + real league-aware `needs_attention`/`overstocked`) with no cross-check. Fixed in both `roster_grading.py` and the duplicate copy in `post_draft_analysis_service.py`: the real league-aware signal now suppresses a contradicting legacy entry for the same position. Live-verified against the user's real Optis Titans roster. Committed as `1efe7f9`.
- [x] **RB quality/units bug + waiver-wire dead-source + stale-variable bug, all fixed (2026-09-05, same session)** — user reported "Excellent RB depth" felt wrong given real RB quality, and the League Detail Waiver Wire tab was still empty. Found three real bugs: (1) ESPN's `projected_points` is a season TOTAL, but quality thresholds in `_identify_strengths_weaknesses` were written for weekly per-game numbers (~17x off) — added `NFL_SEASON_GAMES` constant in `grading.py`, normalized in both copies + `bench_depth_notes`; (2) quality was averaged across the WHOLE rostered group instead of real starters — now uses only the top-N (N = real per-league starter count) players by projection; (3) `league_management_service.py`'s waiver-rec methods sourced from the empty local `Player` table — swapped to the live `WaiverWireService.get_live_trending_recommendations` (same engine the standalone Waiver Wire page uses). Fixing #3 surfaced a 4th, independent, previously-undetected bug in `waiver_wire_service.py` itself: a stale Python for-loop variable meant every recommendation in a weighted batch got the LAST candidate's position baked into its roster-need reason text, regardless of its own real position — fixed + regression-tested (confirmed test fails without the fix). One residual limitation disclosed to the user, not silently claimed as solved: this specific roster's bench RBs still clear the "excellent" bar because ESPN's own season projections for them aren't actually low in raw points — a true committee/role-security read would need a workload signal (snap/carry share) this app doesn't have for RBs (only WR/TE have `target_share`). Full suite 124 passed, `npm run build` clean, live-verified via curl against the real Optis Titans roster. Committed as `1efe7f9`.
- [x] **Real ESPN comprehensive analysis (2026-09-05)** — `get_comprehensive_league_analysis` was Yahoo-only; ESPN/Sleeper honestly 400'd, which the user hit directly on their own ESPN league's Waiver Wire/Trade Center tabs. Built a full ESPN branch mirroring Yahoo's, reusing existing `espn_service_enhanced.py` methods (no new ESPN API calls needed). Live-verified against the user's real Optis Titans league via curl and in-browser. See [[project_espn_comprehensive_analysis_ask]]. Committed as `1efe7f9`.
- [x] **Scoring tab made read-only + Overview redesign (2026-09-05)** — user asked for the League Detail Scoring tab to be read-only (no configure/customize) pulling full real settings, and for Overview to show rostered players + "pertinent news." Scoring tab rewritten with zero edit affordances, now shows the complete real scoring breakdown (previously partial) plus new real roster-construction info (starters/bench/roster size/PPR/format) via a `roster_settings` addition to `GET /league-scoring/league/{id}`. Overview got a real "Your Roster" grid and a "Player Alerts" section — investigated a real per-player news feed first and confirmed none exists (a prior scraper path is an explicitly-disabled mock that fabricates text), so alerts use ESPN's real per-player `injury_status` instead of inventing news. Found and fixed a real bonus bug in the process: the Overview "Injuries" tile had been silently dead since built (`leagues.py::GET /{id}/roster-analysis` never populated `injury_concerns` at all). Full suite 124 passed, builds/lints clean, live-verified against the real Optis Titans league. Committed as `1efe7f9`.
- [x] **Waiver Wire real-availability fix (2026-09-05)** — user reported MarShawn Lloyd recommended despite not being available; confirmed real (he's rostered by another team in the user's real ESPN league). Root cause: the live waiver engine (`WaiverWireService.get_live_trending_recommendations`) sources from Sleeper's *global* trending feed with no per-league availability check — only ever excluded the user's own roster, not the whole league's. Fixed with a new optional `available_player_names` filter, wired into every caller via real free-agent APIs (ESPN/Yahoo `get_available_players`; Sleeper derived by excluding anyone rostered by any team in the league, best-effort against the sparse local Player table). Live-verified: Lloyd now correctly excluded everywhere, every remaining recommendation cross-checked as a genuine free agent. 2 new regression tests, full suite 126 passed. Committed as `1efe7f9`.
- [~] **Gridiron Terminal frontend redesign (sessions 8–9)** — full design-token overhaul + dark mode (`useTheme`, `ThemeToggle`, `[data-theme]` + `prefers-color-scheme`), site-wide class migration, copy rewrites, emoji sweep, filled-volt primary buttons, richer PlayerCard meta row, dark-safe status surfaces, `?tab=` param on LeagueDetailPage (`1efe7f9`). Session 9 finished the follow-up list: dark-mode contrast pass across data pages + `.pos-badge-*` classes + button contrast (`132bfa8`), theme-aware charts via `useChartColors` + `--viz-*` palette (`8362eeb`), **This Week screen** as a real ESPN-wired tab + progressive `/leagues/:id` render (`9083966`). All committed + pushed. Still open: charts not visually verified in a running browser; This Week optimizer is v1 greedy. See `handoff.md`.
- [~] **STRATEGIC PIVOT (2026-09-08): remove draft assistant, refocus in-season.** Founder-approved. Plan: "The Season Is the Product" artifact (https://claude.ai/code/artifact/1c5aaa93-1db3-4ae4-9dd7-36f032e3128a) + [[project_draft_assistant_decommission]]. **Backend teardown DONE** (`d58b8d2` extract shared helper → `roster_requirements.py`; `2d40783` delete Live/Mock draft endpoints+services+tests, new `player_pool.py`; 101 tests pass). **Frontend teardown DONE** (`fb37f71`, pushed) — deleted DraftPage/LiveDraftPage/`components/draft/`, dropped the `draft` api export + Mock*Payload/Result types (kept draft-session-history types + `users` export for DraftHistoryPage), `/draft` + `/live-draft` now redirect to `/leagues`, nav items + HomePage/AuthPage links repointed to in-season destinations; build + lint clean. Kept: Post-Draft Analysis, Draft History (`/users/me/drafts*`), `DraftSession` model, ESPN rankings script. **Nav re-cut DONE** (`804757e`): top nav = This Week · Leagues · Waivers · Trades · Players; "More" = Reports · Content · Analytics · Advanced Analysis. New `/this-week` (redirects to first league's This Week tab) + `/reports` hub (Post-Draft / Draft History / Historical). Blog dropped from nav, linked from Content.
- [x] **ESPN league-analysis speed pass** (`8705af1`) — the "`/leagues/:id` takes ~25-30s" perf note (session 8) is fixed. `get_comprehensive_league_analysis` takes `sections` + runs slices concurrently; `/waiver-recommendations` and `/trade-suggestions` now compute only their own slice (previously each ran all 5 and discarded 4 — and the page fires both). Independent ESPN reads + per-position AI calls gathered; `_get_league` got a per-key construction lock. Warm timings vs real Optis Titans: waiver 4.6s / trade 3.1s / this-week 0.8s. 102 tests pass.
- [x] **Backend deployed to Render, frontend wired up (session 11, 2026-09-09)** — `fantasy-fb-assistant.onrender.com` live (Docker, `render.yaml`, autoDeploy on push); Vercel `VITE_API_URL` points at it. Fixed the network-error bug (Vite bakes env vars at build time, old bundle had `localhost:8000` baked in). Snapshot cache (`league_snapshots` table + `serve_swr` stale-while-revalidate) added for This Week/roster/standings. Mobile-first responsive pass across all data pages. Player headshots/team logos (`PlayerAvatar`) + injury badges added, matching ESPN app polish.
- [x] **Waiver intelligence + H2H matchup + Overview removed (session 12, 2026-09-14)** — real waiver-position panel (FAAB/priority rank, fixed a real `acquisition_budget` bug reading a nonexistent espn_api field); real per-league competition signal (`league_competition.py` — other teams' actual roster thinness + this-week bye/injury acute need, named not just counted); real drop-candidate + value-delta pairing on the Top Waiver Target card (`pick_drop_candidate`); This Week table got team/actual-points columns + IR moved below bench; fixed an invisible D/ST badge (light-bg+light-text in dark mode); real season-schedule "Matchups" tab rebuilt from scratch (previous one was dead code — `matchupData` was never set to anything but `null`); side-by-side H2H matchup view (`opponent_lineup` was already fetched by `get_week_matchup`, just never surfaced) with a My-Lineup/Matchup toggle. Removed the "Overview" tab after a 4-lens panel review confirmed every real thing on it was duplicated elsewhere or a dead stub (`/insights` endpoint returns a placeholder sentence, not real content). Repurposing Roster Analysis into a "Bye Week Radar" was the panel's pick but NOT built — found a real espn_api bug (`on_bye_week` wrong for future weeks) during verification; see [[project_espn_api_bye_week_bug]]. 102 tests pass throughout; frontend build/lint clean.
- [x] **Dead /blog router + legacy content_service removed (session 13, 2026-09-15)** — picked "kill the mock news scraper" off the backlog; found the actual fabrication was already fixed in `f791d8a` (2026-08-23). What remained was dead code: `blog.py` (7 routes) + `app/services/content_service.py`, confirmed zero frontend callers (frontend only calls `/content/*`), and its own live-scrape endpoints confirmed dead on top of that (fantasypros.com 404, nfl.com permanent redirect). Both deleted, `router.py` updated, `scraper_service.py` trimmed to the one method still called. Docs (`API_GUIDE.md`, `DECOMMISSION_TASK_LIST.md`) updated to match, including a separately-already-fixed `content-stats` 500 claim found stale in the same pass. 102 tests pass. Committed `268a791`, pushed.
- [x] **Bye Week Radar shipped, real espn_api bug root-caused (session 14, 2026-09-15)** — the espn_api bug blocking this ([[project_espn_api_bye_week_bug]]) turned out deeper than first suspected: `League.box_scores(week=N)` silently fetches (and mislabels) the CURRENT week's data for any future `week` requested, rather than erroring — confirmed live by diffing `box_scores(week=2)` vs `box_scores(week=11)` mid-season and finding them byte-for-byte identical. Fix sidesteps `box_scores()`/`on_bye_week` entirely: `espn_service_enhanced.get_bye_week_radar()` crosses ESPN's real per-team `byeWeek` field against `team.roster` directly (no week-clamping in that path). Wired end-to-end (snapshot builder, `GET /leagues/{id}/bye-week-radar`, new card in the Roster Analysis tab). Live-verified against the real Optis Titans league (correctly flags Seahawks players' real week-11 bye) and in-browser in both themes. 102 tests pass, frontend build/lint clean. Committed `848fe1c`, pushed.
- [x] **Priority-aware waiver bid tiers (session 15, 2026-09-15)** — each waiver recommendation now gets a real, actionable claim suggestion (`WaiverWireService.compute_bid_tier`), reading its priority tier against the user's actual ESPN waiver standing (`get_waiver_position`): a suggested $ bid as a real fraction of actual remaining FAAB budget, or use-claim/hold-priority advice for rolling-priority leagues based on real `waiver_rank`. Wired through both `waiver_wire.py` personalization paths and `league_management_service._get_espn_waiver_recs`; `None` (not a placeholder) when no real waiver position is known. Frontend renders it on WaiverWirePage + LeagueDetailPage's Waiver Wire tab. 109 tests pass. Committed `2d9728a`, pushed.
- [x] **Weekly digest notifications, scheduled via GitHub Actions (session 15, 2026-09-15)** — the highest-leverage backlog item: a real, schedule-driven digest (one notification per connected league, real top live-Sleeper waiver target + real ESPN bye-week-radar risk), dedupe-keyed per ISO week. This app has no background worker (Celery/Redis removed) and Render's free tier has no cron, so the actual trigger is `.github/workflows/weekly-digest.yml` (free GitHub Actions scheduled workflow, Tuesday 13:00 UTC) calling new `POST /notifications/generate-weekly-digest`, guarded by a shared `DIGEST_CRON_SECRET` (not user auth — fans out across every user). **Manual setup still needed, not done by this session:** set `DIGEST_CRON_SECRET` to the same real random value in both the Render dashboard and this repo's GitHub Actions secrets (`gh secret set DIGEST_CRON_SECRET` or the repo Settings UI) — the endpoint refuses every request until that's set. 7 new tests, 116 total pass. Committed `f025503`, pushed.

- [x] **Optimizer v2 + real trade finder + Sleeper roster analysis (session 16, 2026-09-16)** — three ranked backlog items, each tested + live-verified. (1) `this_week_service.start_sit_confidence()`: per-starter confidence tier from real projection gap to best bench alternative + live injury status; optimizer swaps get a matching `confidence` field so a big point-gap swap into an injured replacement reads as `risky`, not equal to a clean upgrade. (2) Found ESPN's `_get_espn_trade_recommendations` was pure AI fabrication (invented `target_player`/`offer_players` knowing only `len(teams)`) — replaced with `trade_finder_service.find_trade_suggestions`, a deterministic matcher over real rosters `get_league_teams` already returns; every suggestion names two real rostered players in a real two-way value swap. (3) Sleeper roster analysis was an honest "not yet implemented" stub — built `_build_sleeper_roster_analysis_snapshot` resolving Sleeper's raw roster-id lists against the real player catalog through the shared `grade_roster` engine; live-testing against a real Sleeper league found and fixed a real bug (Sleeper's `injury_status: "NA"` was being misread as an injury concern). 136 tests pass (was 116). Founder chose to stop here rather than continue to the remaining two backlog items this session.

- [x] **Yahoo API unlocked + full ESPN/Yahoo parity (session 17, 2026-09-22–23, `/goal` complete)** — Yahoo granted real Fantasy Sports API access after ~2.5 months blocked ([[project_yahoo_fantasy_api_locked]]); first real Yahoo data exposed and fixed a long chain of parsing bugs. Now real for Yahoo: connect + automatic token renewal (`yahoo_tokens.py`), standings, roster grading (quality, not just composition), This Week (scoreboard, win prob, optimizer, start/sit), Matchups history, Bye Week Radar, scoring rules, waiver recs (300-player free-agent pool, ownership, projections, byes, drop candidates, bid tiers, competition badges), real trade suggestions, digest byes. Yahoo per-player projections come from Sleeper's feed scored with each league's real Yahoo rules (`weekly_projections.py`). Shared bugs fixed along the way: IR players in the optimizer bench pool; FLEX requirement mis-allocation. 167 tests pass. See `handoff.md`.

Ranked in-season backlog after the Yahoo parity `/goal` resolves: weekly recap + shareable card (new feature, needs a design decision on what "shareable" means), consolidate the 3 analysis pages (`AdvancedAnalysisPage`/`AnalyticsPage`/`HistoricalPage` — audit real-vs-fabricated first, `AnalyticsPage`'s ML predictions depend on per-player historical stats noted elsewhere as sparse). Also holding Vercel free tier ([[project_vercel_free_tier]]) — bundle diet.
- [ ] **Decommission audit findings** (`DECOMMISSION_TASK_LIST.md`) — large backlog of real, evidence-based bugs/dead-code, not yet started as a dedicated pass. See open items below.
- [ ] **Push alerts (in-app MVP)** — scoped, not built (`DEFERRED_FEATURES_CHECKLIST.md`).
- [~] **Multi-platform league analysis** — ESPN fullest; Yahoo now real for connect/standings/waivers/roster-grading/roster-import as of session 17, This Week still open; Sleeper still deliberately returns "not implemented" for This Week/comprehensive-analysis rather than fake data. See [[project_yahoo_sleeper_roster_grading_ask]].

## Founder-only / blocked items (not agent-workable)

- Rotate the ESPN session cookie that was briefly exposed in git history (needs the founder to log out/in to ESPN).
- Decide whether to rewrite local git history to purge that old commit.

## Decommission-list backlog: fully re-verified, fully stale (2026-08-29)

`docs/audits/DECOMMISSION_TASK_LIST.md`'s entire specific-bug backlog (11 items) was re-checked against current code this session. **Every single one is already fixed** — each fix site carries a docstring/comment explaining the original bug and, where identifiable, the fixing commit:

- Platform-enum string comparisons (`leagues.py`, `league_scoring.py`, `post_draft.py`, `league_management_service.py`) — all correctly use `.value.upper()`.
- `leagues.py::GET /{id}/roster-analysis` — real `team_id`/`season`, real `grade_roster` engine (`cd3b6bd`).
- `leagues.py::GET /{id}/standings` — real auth, scoped to caller's `UserLeague`, real per-platform data.
- `post_draft_analysis_service.py` roster lookup — already uses SQLAlchemy `or_()`, not Python `or`.
- `league_scoring.py::POST /compare-players` IDOR — real ownership check (`LeagueScoring` joined to `UserLeague.user_id`).
- `content.py::GET /content-stats` — `func` correctly imported from `sqlalchemy`, used as `func.count(...)`.
- `game_situation.py` models — use `func.now()`, not string `"now()"` (its own migration, `80e42eca85d0`, exists).
- `optimization_service.py::optimize_lineup` FLEX constraint — reworked to a combined-pool constraint (comment documents the exact old infeasibility bug).
- Season hardcoding in `matchup_analysis_service.py`, `players.py`, `draft_assistant_service.py::_calculate_round` — all derive real season/league-size instead of hardcoding.
- `ai_service.py::generate_multi_perspective_content`/`generate_consensus_recommendation` — both route through `_generate_with_fallback`.
- **Alembic fresh-DB migration chain** — verified empirically: spun up a throwaway local Postgres cluster (not the dev DB), ran `alembic upgrade head` against a blank database, all 20 migrations (including 2 merge heads) applied cleanly with zero errors.

**Lesson: this audit doc decays fast and should not be trusted as a live task list without re-checking each item against current code first.**

## Remaining open backlog (not bugs — product/architecture decisions, not attempted)

**Duplicate/orphaned implementations needing a canonical-path decision** (from `docs/audits/DECOMMISSION_TASK_LIST.md`, "which version wins" calls, not pass/fail bugs):
- [x] **Game situations — resolved 2026-08-29.** `enhanced_game_situation_service.py` (via `POST /game-situations/enhanced-analysis`) is now the sole implementation. Removed the dead duplicate `AdvancedAnalysisService.analyze_game_situations`/`POST /advanced-analysis/game-situations` (confirmed zero callers first) plus its 8 dedicated helper methods and now-dead imports. A third `historical.py` copy had already been removed in an earlier session. Verified: `pytest` 73/73, backend imports clean, `npm run build` green, no remaining references anywhere in the repo.
- [ ] Advanced analysis: 2 stat engines (`advanced_analysis_service.py`, wired vs. `advanced_historical_service.py`, better stats but unwired). *Not yet re-verified.*
- [ ] Matchup analysis: `matchup_analysis_service.py` used internally but its own endpoint file has zero frontend callers. *Not yet re-verified.*
- [ ] Player AI analysis: 2 parallel paths (`POST /players/{id}/analysis`, frontend-used, vs. `POST /players/enhanced/{id}/analysis`, unused). *Not yet re-verified.*

## Reference docs (all re-verified as of the last docs pass, per CLAUDE.md)

Now organized under `docs/` (moved out of repo root this session): `docs/guides/AUTHENTICATION_GUIDE.md`, `docs/guides/DRAFT_ASSISTANT_GUIDE.md`, `docs/guides/ENHANCED_PLAYER_DATA.md`, `docs/testing/YAHOO_INTEGRATION_TEST.md`, `docs/testing/LIVE_DRAFT_TEST.md`, `docs/guides/API_GUIDE.md`, `docs/guides/STYLE_GUIDE.md`, `docs/audits/DECOMMISSION_TASK_LIST.md` (point-in-time audit — check git log before trusting an item as still-current). `README.md` and `CLAUDE.md` stay at repo root (tool/GitHub convention); `current-state.md`/`handoff.md`/`memory.md` stay at root by choice, for quick access during the `/clear`-resume workflow.
